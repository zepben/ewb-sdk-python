name: Python Lib Build

on:
  pull_request:
  workflow_dispatch:

jobs:
  run:
    uses: zepben/.github/.github/workflows/python-build.yml@python3.10
    with:
      private: false
    secrets: inherit
